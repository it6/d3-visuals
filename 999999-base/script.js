window.onload = () => {
  const width = window.innerWidth,
    height = window.innerHeight;

  const margin = {
    top: 20,
    bottom: 20,
    right: 30,
    left: 30
  };

  const svg = d3.select('body').append('svg');
};
